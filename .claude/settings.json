{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT_COMMAND\" | grep -qE '^git commit'; then npm run lint -- --max-warnings 0 && npm run typecheck || (echo '\\n❌ Lint or typecheck failed — fix before committing.' && exit 1); fi"
          }
        ]
      }
    ]
  }
}
